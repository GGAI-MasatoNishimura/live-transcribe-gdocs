/**
 * Phase 1: ローカル中継のエントリの場所だけ確保する。
 * WebSocket 待受・STT・Docs API は以降のフェーズで追加する。
 */
console.log("[relay] MVP Phase1 scaffold. Run `npm start` from relay/ to verify Node runs.");
console.log("[relay] Next: Phase 2+ adds WebSocket server and Chrome extension connection.");
